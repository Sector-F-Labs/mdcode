[package]
name = "mdcode"
version = "0.1.0"
edition = "2024"
description = "Extract fenced and inline code blocks from Markdown"
license = "BSD-3-Clause"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
